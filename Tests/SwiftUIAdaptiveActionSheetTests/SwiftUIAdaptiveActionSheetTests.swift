import XCTest
@testable import SwiftUIAdaptiveActionSheet

final class SwiftUIAdaptiveActionSheetTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUIAdaptiveActionSheet().text, "Hello, World!")
    }
}
